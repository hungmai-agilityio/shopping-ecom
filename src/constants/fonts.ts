import { Poppins, Roboto } from 'next/font/google';

export const popping = Poppins({
  subsets: ['latin'],
  weight: ['400', '500', '700']
});

export const roboto = Roboto({
  subsets: ['latin'],
  weight: ['400', '500', '700']
});
